
match    := expr
expr     := (group | or_op  | word)
group    := (expr)[<expr>]
word     := subexpr | char[word] | char [<expr>]
subexpr  := char* | char<counter>
char     := any_char | a --> z | A --> Z | digit
counter  := {digit}
any_char := .
digit    := 0->9[digit]
or_op    := word + word
ignor    :expr"\I"

